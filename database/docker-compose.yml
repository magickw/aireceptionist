version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: aireceptionist
    ports:
      - '5435:5432'
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
