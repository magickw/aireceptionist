{
  "buildCommand": "cd frontend && rm -rf .next && npm run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",
  "installCommand": "npm install && cd frontend && npm install",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
