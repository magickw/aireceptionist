'use client';
// This is a placeholder for the customers page.
import { Container, Typography } from '@mui/material';
export default function CustomersPage() {
  return <Container><Typography variant="h4" sx={{mt:4}}>Customers</Typography></Container>;
}
