{
  "name": "ai-receptionist-pro",
  "version": "2.0.0",
  "description": "AI-powered business phone management platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && uvicorn app.main:app --reload\"",
    "build": "cd frontend && npm run build",
    "start": "concurrently \"cd frontend && npm run start\" \"cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000\"",
    "install:all": "cd frontend && npm install && cd backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
